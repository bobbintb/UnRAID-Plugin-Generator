ENTITIES:
  name : bobbintb.system.dirt
  author : bobbintb
  repo : UnRAID-DiRT
  tag : test
  launch : Settings/Deduplication in Real-Time
  pluginURL : https://github.com/&author;/&repo;/releases/download/&tag;/&name;.plg # Uses other entities
  packageURL : https://github.com/&author;/&repo;/releases/download/&tag;/&name;.txz # Uses other entities
  source : /boot/config/plugins/&name;/&name; # Uses other entities
  icon : fa-search-minus
  min : 6.1.9
  version : 2025.02.11
  MD5 : b31ca4f4cc86325d132739c93f79b922
  description: 'A "cool" & useful plugin' # For PLUGIN attribute escaping
  myKnownEntity: "Known Entity Value for CDATA" # Renamed slightly for clarity
  # testEntity is NOT defined here, so &testEntity; in rc.dirt is not a known DTD entity

CHANGES: ./README.md

FILE:
- #STARTUP SCRIPT (Existing INLINE - for std_escape)
  '@Name': /etc/rc.d/rc.dirt
  '@Mode': '0775'
  INLINE: ./.plugin/rc.dirt # Content: echo "&testEntity; & < > \"quotes\"";
- #SOURCE PACKAGE (Existing URL/MD5 with entity refs)
  '@Name': "&source;.txz" # Attribute with entity ref + literal
  '@Run': "upgradepkg --install-new --reinstall"
  URL: "&packageURL;" # Child tag with entity ref
  MD5: "&MD5;"        # Child tag with entity ref
- #GENERIC TAG TEST (New child tag with special chars)
  '@Name': 'genericTest'
  Details: "Contains < & > symbols"
- #CDATA TEST (New)
  '@Name': 'cdataTest'
  CDATA: './test_cdata_final.txt' # Content: raw < & > data &myKnownEntity; and &description;
- #CO-OCCURRENCE TEST (INLINE WINS - New)
  '@Name': 'coOccurrenceTest1'
  Comment: 'INLINE should win as it is last.'
  CDATA: './test_cdata_final.txt'
  INLINE: './test_inline_final.sh' # Content: echo "INLINE version wins: &description;";
- #CO-OCCURRENCE TEST (CDATA WINS - New)
  '@Name': 'coOccurrenceTest2'
  Comment: 'CDATA should win as it is last.'
  INLINE: './test_inline_final.sh'
  CDATA: './test_cdata_final.txt'
